(library
 (public_name estoril)
 (libraries base sedlex zarith camomile batteries xmlm)
 (modules lexer parser location syntax)
 (preprocess
  (pps sedlex.ppx)))

(menhir
 (flags --explain)
 (modules parser))

(env
 (dev
  (flags
   (:standard -w +a-4)
   (:standard -warn-error -a+31))))
